// TODO: This page handles the checkout redirect to Stripe
// Receives selected tickets, creates Stripe Checkout Session, redirects
export default function CheckoutPage() {
  return (
    <main>
      <p>Checkout — awaiting design</p>
    </main>
  );
}
