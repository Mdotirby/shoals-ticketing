"use client";

// TODO: Design ticket selector widget
// Used on /events/[id] page
// Shows ticket types (GA/VIP/Table) with +/- quantity controls
// Displays running total
// "Checkout" button at bottom
export default function TicketSelector() {
  return (
    <div className="ticket-selector">
      <p>Ticket Selector — awaiting design</p>
    </div>
  );
}
