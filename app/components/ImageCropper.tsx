"use client";

// TODO: Wraps react-easy-crop library
// Used on admin event create/edit pages
// Accepts image URL, returns crop coordinates (x, y, width, height)
export default function ImageCropper() {
  return (
    <div className="image-cropper">
      <p>Image Cropper — awaiting design + react-easy-crop integration</p>
    </div>
  );
}
