"use client";

// TODO: Wraps html5-qrcode library
// Camera viewfinder with onScan callback
// Used on /admin/scan page
export default function QRScanner() {
  return (
    <div className="qr-scanner">
      <p>QR Scanner — awaiting design + html5-qrcode integration</p>
    </div>
  );
}
