// TODO: Design login page
// Email + password form → Supabase Auth signInWithPassword
// After login: check must_change_password → redirect to /admin/settings or /admin
export default function AdminLoginPage() {
  return (
    <main>
      <p>Admin Login — awaiting design</p>
    </main>
  );
}
